apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: web-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: web-repo
  policy:
    semver:
      range: ">= 1.0.0"