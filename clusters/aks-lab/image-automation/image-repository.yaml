apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageRepository
metadata:
  name: web-repo
  namespace: flux-system
spec:
  interval: 1m
  image: fluximages.azurecr.io/nginx
  secretRef:
    name: registry-credentials
